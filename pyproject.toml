[project]
name = "Cryptomania"
version = "0.1.0"
description = "A library to code and encode messages and strings!"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "termcolor>=3.3.0",
]
